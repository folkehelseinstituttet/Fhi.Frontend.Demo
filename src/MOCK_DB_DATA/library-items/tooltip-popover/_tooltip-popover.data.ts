import { LibraryItem } from 'src/app/views/shared/models/library-item.model';

import { Tooltip } from './tooltip';
import { Popover } from './popover';

export const TootipPopoverData: LibraryItem[] = [...Tooltip, ...Popover];
